{{- /* Preview builds are public and crawlable but are not the site, and their
       absolute URLs still point at production — see the comment in
       layouts/_default/baseof.html. A preview that said "Allow: /" and then
       advertised production's sitemap would be inviting crawlers to treat a
       throwaway deployment as the real thing. */ -}}
{{- $preview := in (slice true "true" "1") site.Params.preview -}}
{{- if $preview -}}
User-agent: *
Disallow: /
{{- else -}}
# Search engines are welcome. Training AI models on this work is not.
#
# This file is the polite half. Cloudflare enforces the same preference at the
# edge — Security > Settings > Bot traffic > Configure AI bot policies, with
# Training set to Block — because robots.txt is voluntary and a crawler that
# ignores it is exactly the crawler this is aimed at.
#
# Keep "Bot Preference Sync" OFF there. It prepends Cloudflare's own managed
# robots.txt to this one, zone-wide, which silently replaced the client hub's
# blanket Disallow with "Allow: /" and served it without Access. This file
# should be the only robots.txt either site serves.
#
# Deliberately NOT blocked, though Cloudflare's managed list includes them:
# Baiduspider and PetalBot are web search, not training. Blocking AI training
# is not a reason to drop out of two search engines.
#
# Agent crawlers — the ones fetching a page to answer somebody's question now —
# are also allowed, on purpose. Someone asking an assistant who builds
# portfolio sites for artists should be able to find us. Don't train on the
# work; do send people to it.

User-agent: *
Content-Signal: search=yes,ai-train=no,use=reference
Allow: /

# Training crawlers. The well-documented ones are named here so the intent is
# legible; Cloudflare's enforcement covers a longer and changing list, and this
# file is not trying to mirror it.
User-agent: GPTBot
Disallow: /

User-agent: ClaudeBot
Disallow: /

User-agent: anthropic-ai
Disallow: /

User-agent: Claude-Web
Disallow: /

User-agent: Google-Extended
Disallow: /

User-agent: Google-CloudVertexBot
Disallow: /

User-agent: Applebot-Extended
Disallow: /

User-agent: meta-externalagent
Disallow: /

User-agent: CCBot
Disallow: /

User-agent: Bytespider
Disallow: /

User-agent: cohere-ai
Disallow: /

User-agent: MistralAI-Training
Disallow: /

User-agent: Diffbot
Disallow: /

User-agent: omgili
Disallow: /

Sitemap: {{ "sitemap.xml" | absURL }}
{{- end }}
